<div class="col-md-6 col-md-offset-3">

  <div class="login-form">
<div class="center">
<h5> All conversations </h5>
</div>

<% @conversations.each do |conversation| %>
	<div class="form-group">
	<h4 class="current-category" style="color:blue"> subject:<%=link_to conversation.subject, conversation_path(conversation) %></h4>

	</div>
	<%end%>

	<%= link_to "New message", new_conversation_path,  class: "btn btn-default" %>
	
	<% unless @conversations.empty? %>

<nav class"navbar-fixed-bottom">
  <ul class="pager">
    <li class="previous " <%= will_paginate @conversations %> <span aria-hidden="true">&larr;
   
  </ul>
</nav>
<% end %>

	</div>
	</div>

	