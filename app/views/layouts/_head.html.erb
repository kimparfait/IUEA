<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">UMUFACE</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        
        <li><a href="/pages/about">About</a></li>

          <% if user_signed_in? %>
        
         <li><%= link_to "Edit Profile", edit_user_registration_path %></li>
          <li><%= link_to 'Profile', current_user %></li>
           <li class="round-image-50" ><%= image_tag(current_user.image.url(:thumb)) %></li>
        
             <li><%= link_to "Messages", conversations_path %></li>
        <li><%= link_to "Sign Out", destroy_user_session_path(current_user), method: :delete %></li>
          <% else %>
         <li class=""><%= link_to "Hi!Sign Up", new_user_registration_path %></li>


         <li><%= link_to "Sign In", new_user_session_path %></li>
         <%end%>
      </ul>
       
 </ul>
 
      

 <ul class="nav navbar-nav navbar-right">
       <% if user_signed_in? %>
      <li><%= link_to 'New Post', new_post_path  %></li>
      <% else %>
       <%end%>

        <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"  aria-expanded="false">All Categories<span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                 <% Category.all.each do |category|%>
                <li class="<%='active' if params[:category] == category.name %>" > 
                <%= link_to category.name, posts_path(category: category.name), class: "link" %></li>
                 <%end%>
              </ul>

      </ul>

            
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>